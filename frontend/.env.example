# Base URL of the Chamie backend API.
# - Leave empty for local dev (the Vite dev server proxies /api -> localhost:8080).
# - Set to the backend origin in production, e.g. https://api.example.com
VITE_API_BASE_URL=
