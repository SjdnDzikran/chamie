{
  "name": "whatsapp-support-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "kapso": "kapso",
    "sync:secrets": "bun run scripts/sync-secrets.js",
    "validate": "bun run scripts/validate.js",
    "test": "bun test"
  },
  "devDependencies": {
    "@kapso/cli": "^0.15.0",
    "@kapso/workflows": "^0.2.0"
  }
}
